import { DiscountAllocatedPipe } from './discount-allocated.pipe';

describe('DiscountAllocatedPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountAllocatedPipe();
    expect(pipe).toBeTruthy();
  });
});
